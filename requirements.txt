# Pinned to the versions this project was actually tested against. The saved
# pipeline in final_model/ was fitted with these, so bumping scikit-learn or
# xgboost can make the pickle fail to load in the deployed app. If you upgrade
# either one, re-run final_model/build_final_pipeline.py to regenerate the model.
numpy==2.2.1
pandas==2.3.2
scikit-learn==1.7.1
xgboost==3.3.0
matplotlib==3.10.5
seaborn==0.13.2
joblib==1.5.1
shap==0.52.0
streamlit==1.60.0
